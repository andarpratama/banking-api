apiVersion: v1
kind: Service
metadata:
  name: banking-api
  namespace: banking
  labels:
    app: banking-api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app: banking-api

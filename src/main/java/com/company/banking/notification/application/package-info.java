/**
 * notification application — {@link com.company.banking.notification.application.NotificationPublisher}
 * port for cross-feature outbound notices.
 *
 * <p>Current callers: {@code TransferService} (transfer completed). Ready for auth/deposit/withdraw.
 */
package com.company.banking.notification.application;

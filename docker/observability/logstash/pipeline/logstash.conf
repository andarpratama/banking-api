input {
  udp {
    port => 5000
    codec => json
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "banking-api-%{+YYYY.MM.dd}"
    data_stream => false
    ilm_enabled => false
  }
}
